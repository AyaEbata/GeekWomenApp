<%= form_for(geek_woman) do |f| %>
  <% if geek_woman.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geek_woman.errors.count, "error") %> prohibited this geek_woman from being saved:</h2>

      <ul>
      <% geek_woman.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_area :message %>
  </div>

  <div class="field">
    <%= f.label :picture %>
    <%= f.text_field :picture %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
